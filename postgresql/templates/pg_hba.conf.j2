# pg_hba.conf with maximum access
# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Allow all connections from any IP address with any user to any database
host    all             all             0.0.0.0/0               trust
host    all             all             ::/0                    trust
